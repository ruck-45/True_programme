// Navigator object
    // we can determine browser capabilities using navigator object
    // we can know the browser in which the site is opened
    // and the settings allowed in the browser (like flash is on or not? ... etc)


    // some navigator properties
        navigator.appName;
        navigator.appVersion;
        navigator.appCodeName;
        navigator.cookieEnabled; // is cookie enabled : T/F
        navigator.userAgent; 
        navigator.platform; // windows/apple/linux ? 

        navigator.javaEnabled();